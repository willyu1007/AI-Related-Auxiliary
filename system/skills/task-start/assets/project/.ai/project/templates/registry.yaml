# Project registry (template)
#
# This file is created by: `node .ai/scripts/ctl-project-governance.mjs init`
# It is the semantic SSOT for Milestones/Features/Requirements/Tasks.
#
# IMPORTANT:
# - Task progress status is derived from `dev-docs/**/active/<task>/01-status.md` (SoT).
#   Legacy `00-overview.md` is read only when the canonical file is absent.
# - Task IDs are anchored in `dev-docs/**/active/<task>/.ai-task.yaml` (SoT for identity).

version: 1

# Where task bundles live. Omit or leave empty to auto-discover `dev-docs` roots.
task_doc_roots:
  - dev-docs

milestones:
  - id: M-000
    title: Inbox / Triage
    status: in-progress
    description: Triage queue for new or unplanned work.

features:
  - id: F-000
    title: Inbox / Untriaged
    milestone_id: M-000
    status: in-progress
    description: Untriaged tasks live here until mapped to a real feature.

requirements: []

tasks: []
