# Implementation

Create this file only when the realized design is non-obvious, cross-module, migration-sensitive, or operationally significant. Keep the current implementation map; Git history owns superseded states.

## Realization map

| Area | Current behavior | Key paths / entry points | Non-obvious constraint |
|---|---|---|---|
| <!-- module or boundary --> | <!-- what is implemented now --> | <!-- paths, symbols, commands --> | <!-- ordering, compatibility, runtime, or migration fact --> |

## Integration and operations

- <!-- current runtime, deployment, compatibility, migration, or recovery fact -->

Do not record task status, decision history, ordinary TODOs, verification results, or failed attempts here.
