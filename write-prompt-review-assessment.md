# `write-prompt-review.md` 逐项评估

## 总体判断

报告指出了一些有价值的局部改进，但“`write-prompt` 可执行性很低”的主结论偏重。这个
skill 处理的是写作与决策，而不是工具操作；解析执行边界、编写契约、分离指令与输入、
裁剪和验证本身就是可执行工作流。runtime 分支也已经给出调用链检查和行为验证动作。

报告最有价值的发现是需要一次仅使用接收方可见上下文的 dry run。按模型强弱调整详细
程度指向了真实的校准问题，但合理判据应当是接收方能否可靠恢复信息以及偏离的风险，
而不是笼统的强模型或弱模型标签。

## 逐项判断

| 报告建议 | 判断 | 处理 | 理由 |
|---|---|---|---|
| 增加完整的“坏 prompt → 好 prompt”示例 | 部分成立，但优先级被夸大 | 暂缓 | 示例可能帮助识别问题，也可能锚定不相关的 prompt 类型并增加常驻上下文。`task-start` 需要示例，是因为它生成固定治理结构，不能据此推导本 skill 也有同样需求。只有真实使用证明某类失败无法被现有规则纠正时，才增加一个针对性示例。 |
| 将五条裁剪判据拆成编号列表 | 不是实质缺陷 | 不采纳 | 这些判据彼此独立且可以直接执行。编号主要改善人类引用，不会实质改变模型决策，反而增加体例和篇幅。 |
| 增加失败名录 | 部分成立，但与示例建议重叠 | 暂缓 | 报告列出的多数失败已由不可访问上下文、可观察完成条件、consumer contract、schema authority 和裁剪规则覆盖。通用失败名录会产生重复 authority。只应补充有证据表明模型无法从现有规则识别的反复失败。 |
| 从接收方视角验证 | 成立且价值高 | 采纳 | 仅使用接收方可见上下文阅读，是对执行边界最便宜的行为检查。当前 skill 已要求：当前或代表性任务的 rendered boundary 必须让接收方在没有编写对话的情况下明确第一步、取证路径和停止条件。 |
| 按接收方能力校准详细程度 | 问题部分成立，但强弱模型判据过粗 | 调整后采纳 | 详细程度应取决于接收方能否可靠恢复某项决策，以及偏离的后果。当前 skill 已要求对无法可靠恢复或存在实质风险的部分明确决策或步骤；否则优先表达 outcome、boundary 和 observable completion，而不是强加过程。 |
| 删除“模型生成 prompt 不需要额外用户 review”段落 | 不成立 | 保留 | 这是从真实工作流得出的授权规则：不能只因为 prompt 由模型编写，就新增用户审批门槛；实际执行仍由既有工作流授权决定。 |
| 进行真实 prompt 修订的 forward test | 适合作为验证工作 | 单独暂缓 | 带典型缺陷的真实 prompt 可以检验 skill 是否改变行为，但 fixture 和报告默认不应成为交付 skill 的一部分。应在行为置信度值得这项评估成本时单独执行，不能仅因尚未执行就推断正文存在缺陷。 |

## 已应用调整

采纳的意见已经落实到 `system/skills/write-prompt/SKILL.md`：

- 详细程度根据接收方的可恢复性和偏离风险校准，而不是根据笼统的模型能力等级。
- 执行边界验证只使用当前或代表性任务中接收方可见的上下文，并检查第一步、取证路径
  和停止条件。

没有增加示例库或失败名录。原始报告保持不变，作为本评估的输入来源。
